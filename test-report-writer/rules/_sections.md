# 规则索引

本目录包含 test-report-writer skill 的所有规则文件，执行前请按需加载：

| 规则文件 | 说明 | 何时加载 |
| --- | --- | --- |
| `artifact-file-output.md` | 产物文件输出规范 | 每次执行都要读 |
| `quality-assessment.md` | 质量等级评估标准 | 每次执行都要读 |
| `release-recommendation.md` | 上线建议决策规则 | 每次执行都要读 |
